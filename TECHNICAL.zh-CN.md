# Bilimuzhi(Bilimuzhi)技术实现说明(机制级)

> 本文档面向开发者和好奇用户,说明Bilimuzhi的实现机制与架构。**只讲机制,不公开具体接口端点、URL 路径、参数名或域名细节**。

## 1. 架构总览

Bilimuzhi是 Manifest V3 浏览器扩展,由四个入口组成:

- **Side Panel 主界面**:字幕工作区的 UI 组合根;
- **Service Worker 后台**:任务协调、运行时契约与消息路由;
- **Content Script 页面桥**:在视频页中读取播放器状态并注入受控请求;
- **Offscreen 文档**:语音转字幕所需的本地媒体处理。

技术栈:TypeScript + Preact + Vite;无 UI 库、无组件库、无 CSS 框架,样式为手写 CSS 语义变量(浅色/深色/跟随系统)。

## 2. 视频身份锁定

每个视频以 **VideoKey = BVID + CID + 分P** 精确标识。页面 URL 经稳定态解析后绑定身份;获取任何字幕前先完成精确身份校验(aid 归属复核),杜绝串视频。身份不匹配时失败关闭,不缓存未验证结果。

## 3. 字幕获取机制

- **轨道发现**:官方字幕 / AI 字幕 / 多语言轨道,按用户上传 > 官方 CC > AI 字幕的优先级处理;
- **获取路径**:在视频页面的页面上下文中请求 B 站官方字幕接口(浏览器自动携带用户登录态,扩展不读取 Cookie 字符串);
- **原子替换**:新字幕完整验证后才替换;旧字幕及其派生内容(分段/总结/对话/附件)级联删除;
- **任务去重与并发控制**:同一目标同一时刻只允许一个获取任务。

## 4. 语音转字幕(ASR)

- **授权媒体获取**:先获取完整媒体并做完整性校验(拒绝仅试看/不完整媒体);
- **本地分片**:Offscreen + FFmpeg/WASM 在本地分片:安全阈值、目标分片大小、分片重叠、过大分片有界缩短;
- **转写**:分片发送至语音转字幕服务商(免费额度)转写;
- **交叉模式**:奇偶分片轮换首选两个模型,分散各自配额消耗;失败时有限退避与受限同模型重试;
- **合并**:时间戳裁剪/文本去重后合并,提交为当前会话的字幕上下文。

## 5. AI 能力层

统一 Provider 契约:能力探测、流式事件、错误归一。直连用户配置的服务商;每服务商独立 API Key;自定义 Provider 走可选权限流程(授权前不出现)。

## 6. 分段实现

固定可验证卡片结构(标题、起止字幕行、起止时间、内容、类型);广告识别只属于分段,证据不足时保持普通段;时间链接经应用复核后输出。

## 7. 总结实现

三档提示词(简要/平衡/详细);重要观点就近嵌入经复核的时间链接。

## 8. 对话实现

单线历史;一个视频可建多个对话线程;截断后重新生成;附件支持图片与当前时间戳。

## 9. 时间戳跳转实现

时间标记 → 内容脚本桥 → 播放器 seek;时间轴同步模式持续居中当前字幕。

## 10. 数据与存储

- IndexedDB 为权威存储(会话/字幕/归档/回收站/对话/任务);`chrome.storage.local` 仅小型设置与 API Key;
- 事务化、原子提交、级联删除;回收站期限模型(7/30/365 天/自定义/永久);
- 加密备份导出/导入。

## 11. 安全设计

- 四层提示词(安全隔离层不可覆盖):字幕等不可信内容标记为 untrusted 并转义,不执行其中指令;
- Markdown 净化与提示词注入隔离;
- 错误信息脱敏;权限最小化(主机权限仅 B 站媒体域与用户启用的服务商域)。

## 12. 性能

- 虚拟化时间轴;
- **设计目标**为万行字幕(10,000 行)与 500 条消息量级(自动化性能门禁尚未完成,极端规模未做压力测试);
- 重依赖按需加载。

## 13. 明确不做

- 无云端后端、无遥测、无跨设备同步;
- **不支持充电视频/付费内容字幕获取**;
- 不绕过任何登录、付费、地区、DRM 或试看限制。
